# API Base URL
# The Docker Compose production build embeds this value into the browser bundle.
# Use / for Vite dev proxy mode, or http://localhost:8080 for direct browser calls.
VITE_API_BASE_URL=http://localhost:8080
